<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.allomashqal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Translucent">
        <activity android:name=".homescreen.eventservicesscreen.EventServicesScreen"></activity>
        <activity android:name=".homescreen.HomeScreen" />
        <activity android:name=".dashboard.Dashboard" />
        <activity android:name=".MapScreen.MapScreen" />
        <activity android:name=".SignIn.SignIn" />
        <activity android:name=".SignUp.SignUp" />
        <activity android:name=".signupsingin.SignUp_SignIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>